import React from 'react'

const LoadingIndicator = (props) => (
    <img height={props.height} width={props.width} src='https://ui-ex.com/images/transparent-gif-loading-1.gif'/>
)

export default LoadingIndicator